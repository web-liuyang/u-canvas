export interface UPaintOptions {
	canvasId: string;
	componentInstance: any;
}

export class UPaint {
	// protected ctx : CanvasContext;

	constructor(options: UPaintOptions) {
		// this.ctx = uni.createCanvasContext(options.canvasId, options.componentInstance);
	}
}
